<%- include("partials/header") -%>

<h1>Home</h1>
<p><%= start_content %></p>

<!-- <% for(let i=0; i<posts.length; i++){ %>
    <h3><%= posts[i].blog_title %></h3>
    <p><%= posts[i].blog_body %></p> 
<% } %> -->

<% posts.forEach(function(post){ %>
    <h3><%= post.blog_title %></h3>
    <p><%= post.blog_body.substring(0,100) + " ..." %>
        <a id="read_more" href="/posts/<%= post._id %>">Read More</a>
    </p>
<% }); %>

<%- include("partials/footer") -%>